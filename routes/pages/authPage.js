// const express = require("express");

// const router = express.Router();

// const { BASE_URL } = process.env;

// router.get("/", (req, res) => {
//   res.send(`<a href="${BASE_URL}/api/users/google"> Google login </a>`);
// });

// module.exports = router;
